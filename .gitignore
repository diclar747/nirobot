# Artefacto de entrega original, redundante con niro-enterprise-inicial/ ya descomprimido.
niro-enterprise-inicial.zip

# Caché de compilación incremental de TypeScript (se regenera sola).
*.tsbuildinfo

# Archivo de bloqueo de tareas programadas de Claude Code (estado local de la sesión).
.claude/scheduled_tasks.lock
backups/

# Contraseñas/secretos del servidor (facebook.env, etc.): nunca al repositorio.
secrets/
# Scripts SQL puntuales contra la base de producción.
/*.sql
# Paquete de Facebook: el volcado trae datos reales (grupos, chats, contactos); el sistema/.gitignore ya saca data/ y .env.
niro-autofacebook-paquete/base-de-datos/
niro-autofacebook-paquete/sistema/.claude/
